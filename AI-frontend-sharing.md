# 前端AI使用心得部门分享（10-15分钟）

## 0. 分享目标（开场1分钟）

今天不讲模型原理，重点讲三件事：

1. **怎么用**：哪些事情适合交给AI先起草。
2. **何时用**：哪些场景收益高，哪些场景要谨慎。
3. **用了后有什么变化**：时间、质量、沟通成本是否可见下降。

一句话结论：**AI不是替代开发，而是把“低价值重复动作”自动化，把时间还给思考和决策。**

---

## 1. 10-15分钟分享节奏（可直接照读）

- **第1分钟**：为什么要用AI（提效 + 质量 + 协作）
- **第2-10分钟**：8个高频场景（每个1分钟左右，挑6-8个讲）
- **第11-13分钟**：失败案例、风险边界、验收方法
- **第14-15分钟**：团队落地建议 + Q&A

---

## 2. 高频场景（偏操作、可复用）

> 统一原则：AI先出初稿，人来做验收和取舍。  
> 统一口径：收益数据建议用区间，不用绝对值。

### 场景1：需求澄清与任务拆解（会前/评审前）

**什么时候用**
- PRD刚下来，信息不完整、边界不清晰时。

**操作步骤**
1. 把需求原文 + 页面链接 + 约束条件贴给AI。
2. 要求输出：用户路径、边界条件、异常流、待确认问题。
3. 再要求转为研发视角：任务清单 + 依赖项 + 风险点。

**案例（简化）**
- 输入：  
  - “新增优惠券领取弹窗，未登录先拉起登录，已登录直接领取；活动已结束展示已结束态。”
- AI可产出：  
  - 任务拆解：UI状态、登录态判断、领取接口、失败态提示、埋点事件。  
  - 待确认问题：重复领取提示文案、领取频控、活动时间以客户端还是服务端为准。
- 人工修正点：  
  - 增加“弱网重试”和“登录态过期”处理；补齐埋点命名规范。

**收益评估**
- 需求评审准备时间通常可缩短 **30%-50%**。  
- 需求漏项（尤其异常流）可减少 **20%-40%**。

**适用边界**
- 强业务决策问题（补贴策略、风控阈值）不能只靠AI判断。

---

### 场景2：页面/组件脚手架快速起稿（开工前30分钟）

**什么时候用**
- 新页面结构明确，但重复样板代码较多时。

**操作步骤**
1. 提供技术栈（React/Vue/TS/状态管理/请求库）。
2. 指定目录约定和命名规范。
3. 要求AI生成最小可运行骨架：页面、hooks、api、types、样式。
4. 人工立即做一次结构重排，避免“照单全收”。

**案例（简化）**
- 输入：  
  - “用React + TS生成订单列表页骨架，含筛选、表格、分页、空态和加载态。”
- AI可产出：  
  - 页面容器 + 查询表单 + 列定义 + API封装 + 类型定义。
- 人工修正点：  
  - 拆分大组件；统一错误处理；补上权限点控制。

**收益评估**
- 冷启动时间通常可缩短 **40%-60%**。  
- 目录和样板一致性提升明显。

**适用边界**
- 复杂交互（拖拽编辑器、虚拟列表深度优化）仍需人工主导设计。

---

### 场景3：阅读遗留代码与重构建议（接手老模块）

**什么时候用**
- 接手历史模块、缺文档、命名混乱时。

**操作步骤**
1. 把关键文件分段喂给AI（避免一次性贴太多）。
2. 先问“这段代码在做什么”，再问“风险点和重构优先级”。
3. 要求输出“低风险重构清单”，按可回滚粒度执行。

**案例（简化）**
- 输入：  
  - “这个1000+行组件的状态流和副作用关系是什么？”
- AI可产出：  
  - 状态来源图、重复逻辑位置、潜在竞态点。
- 人工修正点：  
  - 结合真实线上日志验证竞态是否真实可复现。

**收益评估**
- 理解陌生模块速度可提升 **30%-50%**。  
- 重构试错成本降低，回归范围更可控。

**适用边界**
- 不要让AI直接“大改全量文件”；优先小步提交、逐步验证。

---

### 场景4：接口联调与Mock数据构造（联调期高频）

**什么时候用**
- 后端接口未齐、字段经常变更、联调阻塞时。

**操作步骤**
1. 提供接口文档或示例响应。
2. 让AI生成可复用Mock数据和错误场景数据。
3. 要求覆盖：空数据、极值、异常码、字段缺失。
4. 补充“字段变更对UI影响清单”。

**案例（简化）**
- 输入：  
  - “根据订单详情接口生成5组Mock：正常、空商品、超长文本、价格异常、权限不足。”
- AI可产出：  
  - 结构完整的Mock样本 + 每组对应预期UI表现。
- 人工修正点：  
  - 把日期、金额、状态码改成与测试环境一致的真实口径。

**收益评估**
- 联调等待期可减少 **20%-35%**。  
- 页面健壮性问题更早暴露。

**适用边界**
- 涉及真实风控/支付签名逻辑，Mock只能辅助，不能替代真实联调。

---

### 场景5：测试用例与回归清单生成（提测前）

**什么时候用**
- 提测前容易漏测边界条件时。

**操作步骤**
1. 输入变更点（功能、影响页面、已知风险）。
2. 要求AI输出：功能回归、异常流程、兼容性、埋点验证。
3. 再让AI按“P0/P1/P2”优先级排序。

**案例（简化）**
- 输入：  
  - “本次改动：列表筛选重构 + 分页逻辑调整 + 新增导出按钮。”
- AI可产出：  
  - 15-20条回归清单（含前置条件、步骤、预期结果）。
- 人工修正点：  
  - 补充公司内部浏览器版本矩阵、权限角色差异。

**收益评估**
- 用例准备时间可缩短 **30%-45%**。  
- 线上低级回归缺陷概率下降。

**适用边界**
- AI生成用例不能替代核心路径的人工探索式测试。

---

### 场景6：Code Review辅助（提PR后）

**什么时候用**
- 需要快速做首轮自检，减少低级问题进PR时。

**操作步骤**
1. 把diff或关键函数发给AI。
2. 指定检查维度：可读性、边界处理、性能、可测试性。
3. 要求“按严重程度排序”给出问题和建议。

**案例（简化）**
- 输入：  
  - “请审查这个列表渲染改动，重点看重复渲染和key稳定性。”
- AI可产出：  
  - 潜在重渲染点、memo使用建议、缺失依赖提示。
- 人工修正点：  
  - 用真实性能面板验证，不把建议当事实。

**收益评估**
- 低级review意见前置解决率提升 **30%+**。  
- 团队review沟通更聚焦业务和架构。

**适用边界**
- 安全、权限、资金相关逻辑必须人工和规范双重审查。

---

### 场景7：文档、周报、复盘自动化（跨团队协作）

**什么时候用**
- 输出频繁、格式固定但内容分散时。

**操作步骤**
1. 提供素材：PR链接、任务单、问题记录、截图说明。
2. 让AI先按模板汇总，再补“风险与下一步”。
3. 人工统一语气和敏感信息审查。

**案例（简化）**
- 输入：  
  - “根据本周3个需求和2个线上问题，生成周报：进展、风险、下周计划。”
- AI可产出：  
  - 结构化周报初稿 + 风险项列表。
- 人工修正点：  
  - 去除含糊表述，补充可量化数据。

**收益评估**
- 非研发编码类文档时间可缩短 **40%-70%**。  
- 沟通信息更完整，跨团队误解减少。

**适用边界**
- 对外正式文档必须人工终审，避免事实错误和表述风险。

---

### 场景8：学习新技术与方案对比（技术选型前）

**什么时候用**
- 需要快速建立某技术的“可用认知”时。

**操作步骤**
1. 明确上下文：现有技术栈、迁移成本、团队经验。
2. 要求AI输出A/B方案对比表（收益、风险、改造量）。
3. 让AI给出“最小试点方案（PoC）”。

**案例（简化）**
- 输入：  
  - “我们要不要引入新的状态管理库？请基于现有Redux项目给出迁移建议。”
- AI可产出：  
  - 保守方案/激进方案对比 + PoC路线。
- 人工修正点：  
  - 结合团队人力、上线节奏、历史包袱做最终决策。

**收益评估**
- 方案前期调研可缩短 **25%-40%**。  
- 讨论更基于结构化证据而非感觉。

**适用边界**
- 选型结论不能只看AI输出，必须结合真实业务约束。

---

## 3. 失败案例与避坑（2-3分钟）

### 失败案例A：提示词太短，结果“看起来对，实际上错”

**现象**
- 只说“帮我写一个XX组件”，AI输出和项目规范不一致。

**原因**
- 缺上下文：技术栈、目录规范、边界条件、验收标准。

**修正**
- 用四段式提示词：**背景 + 约束 + 输出格式 + 验收标准**。

---

### 失败案例B：把AI建议当最终答案，未做验证

**现象**
- 直接复制AI建议导致线上出现边界缺陷。

**原因**
- 缺少最基本的人工校验（类型、异常流、性能、权限）。

**修正**
- 建立“AI输出验收清单”（见下一节），不验收不合并。

---

### 风险边界（部门统一口径）

1. **数据安全**：不要粘贴敏感信息（密钥、用户隐私、商业机密）。
2. **事实正确性**：AI会“合理胡说”，结论必须可验证。
3. **过度依赖**：关键设计和取舍必须由人负责。
4. **版权与合规**：外部代码片段需注意许可证和来源。

---

## 4. 团队落地建议（1分钟）

### 建议1：统一一套“提示词最小模板”

- 每次至少写清：背景、技术栈、限制、输出格式、验收标准。
- 好处：减少来回沟通，提升输出稳定性。

### 建议2：建立“AI输出验收清单”

- 是否符合项目规范？  
- 是否覆盖异常与边界？  
- 是否可测试、可回滚、可观测？

### 建议3：定义提效指标（每月复盘）

- 需求澄清准备耗时
- 脚手架搭建耗时
- 提测前用例准备耗时
- PR一次通过率、返工次数

### 建议4：从小场景试点，不搞“一刀切”

- 先在文档生成、测试清单、脚手架起稿等低风险场景推广。  
- 再逐步扩到重构建议、方案调研等中风险场景。

---

## 5. 可复制提示词模板（即拿即用）

### 模板A：需求拆解

```text
你是资深前端工程师，请基于以下信息做需求拆解。

【需求背景】
（粘贴PRD摘要）

【技术上下文】
- 技术栈：
- 相关页面：
- 接口依赖：

【输出要求】
1) 用户主流程与异常流程
2) 前端任务拆解（按优先级）
3) 待确认问题清单（不少于8条）
4) 风险点与规避建议

【验收标准】
- 必须包含边界条件（空态、失败态、权限不足、超时重试）
- 结果使用表格输出
```

### 模板B：代码改造建议

```text
请作为代码评审助手，分析以下代码片段。

目标：提升可读性与可维护性，不改变业务行为。
请输出：
1) 当前代码主要职责（3-5条）
2) 潜在风险（按严重程度排序）
3) 低风险重构步骤（可分批提交）
4) 每一步对应的回归验证点

限制：
- 不允许一次性大改
- 保留现有对外接口
```

### 模板C：测试清单生成

```text
请根据以下变更生成回归测试清单。

【变更点】
（功能改动摘要）

【输出格式】
- P0/P1/P2分级
- 每条包含：前置条件、步骤、预期结果
- 单独列出异常与边界场景
- 单独列出埋点与日志验证项
```

### 模板D：周报/复盘生成

```text
请根据素材生成一份“对业务方可读”的周报。

素材：
- 本周完成事项：
- 风险与阻塞：
- 线上问题与修复：
- 下周计划：

要求：
1) 先给简版（200字内）
2) 再给详版（分点，含可量化指标）
3) 最后给“需要协同支持”的事项
```

---

## 6. 一页结论（收尾）

- AI最适合先做：**拆解、起稿、整理、检查**。  
- 人最应该主导：**判断、取舍、验收、负责**。  
- 先从低风险高频场景开始，形成团队模板和验收标准，再逐步扩大范围。  

如果只能带走一句话：**把AI当“提效副驾驶”，不要当“自动驾驶”。**

---

## 7. Q&A备用（可选）

1. **问：AI会不会让工程师能力退化？**  
   答：如果只复制粘贴会；如果把它当加速器并坚持验收，反而会强化结构化思考和复盘能力。

2. **问：我们怎么证明真的提效？**  
   答：用固定指标连续记录4周，对比引入前后的耗时、返工率、一次通过率。

3. **问：哪些场景最先落地？**  
   答：优先文档整理、测试清单、脚手架起稿，见效快、风险低。

