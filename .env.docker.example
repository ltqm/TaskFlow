# 复制为仓库根目录 .env 后供 docker compose 读取（勿提交真实 .env）
# copy .env.docker.example .env

# PostgreSQL 容器内 postgres 用户密码
POSTGRES_PASSWORD=请改成强密码

# JWT 签发密钥（至少 32 位随机字符）
JWT_SECRET=请改成至少32位的随机字符串

# 浏览器访问端口（默认 80；若 80 被占用可改为 8080）
APP_PORT=80


