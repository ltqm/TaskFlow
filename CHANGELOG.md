# 变更记录

本仓库采用轻量变更记录：合并前请在 PR 描述中写清「动机与影响面」；以下为面向试用/发版的重要节点摘要。

## [未发布]

- 工程化：根目录 `package.json` 聚合构建脚本（`build:backend` / `build:frontend` / `build:all`）。
- 文档：`README.md` 部署与环境变量说明；`api.md` 与前后端契约对齐。
